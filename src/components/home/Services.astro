---
import Icon from "astro-icon";
import { services } from "../../data-list";
---

<section id="services">
  <h2>Nuestros Servicios</h2>
  <div>
    {
      services.map(({ title, image, points }, index) => (
        <article class="flex gap-6">
          <img
            src={image}
            alt={`Imagen de ${title}`}
            class="flex-1 w-60 h-[80vh] rounded-3xl object-cover"
          />
          <div class="flex-1 flex flex-col justify-between p-6">
            <div class="flex flex-col gap-4">
              <span class="text-7xl">0{index + 1}</span>
              <h3 class="text-5xl">{title}</h3>
            </div>
            <ul class="flex flex-col gap-4">
              {points.map((point) => (
                <li class="text-xl">⚡{point}</li>
              ))}
            </ul>
          </div>
        </article>
      ))
    }
  </div>
</section>
